services:
  - type: web
    name: calendario-impositivo-ia
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GENAI_KEY
        sync: false
    plan: free
